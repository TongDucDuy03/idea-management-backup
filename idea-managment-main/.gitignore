# Dependencies
node_modules
test-results/
playwright-report/

# Secrets — KHÔNG BAO GIỜ commit
.env
.env.*
!.env.example
!*.example
# Ngoại lệ: chỉ chứa cờ build công khai (GENERATE_SOURCEMAP), không có bí mật.
# Phải commit thì cấu hình mới theo code lên server.
!/.env.production

# Build artifacts
build
backend/dist

# Uploads người dùng
backend/uploads

# Logs / OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db
